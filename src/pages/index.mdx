import { Flex } from "@theme-ui/components";
import { Layout, SEO, SocialMediaLinks } from "../components";

export default ({ children }) => <Layout>{children}</Layout>; // Layout

<SEO title="FANGA" />;

<Flex
  p={4}
  m={4}
  sx={{
    flexDirection: "column",
    justifyContent: "space-around",
    alignItems: "center",
    height: "90vh",
  }}
>
  <AnimatedTitle />
  <SocialMediaLinks />
</Flex>
